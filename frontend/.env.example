# Public site URL (no trailing slash). Required for canonical, Open Graph, and Twitter image URLs.
# Example: https://daveswimschool.ca
VITE_SITE_URL=

# Optional: path under public/ for share previews (e.g. og-default.png). Defaults to /Logo-Small.svg.
# VITE_OG_IMAGE_PATH=/og-default.png
