# Public site URL (no trailing slash). Required at build time so index.html gets absolute
# canonical, og:url, and og:image tags (Discord/Facebook/iMessage crawlers do not run JS).
# Example: https://daveswimschool.ca
VITE_SITE_URL=
